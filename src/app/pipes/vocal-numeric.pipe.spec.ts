import { VocalNumericPipe } from './vocal-numeric.pipe';

describe('VocalNumericPipe', () => {
  it('create an instance', () => {
    const pipe = new VocalNumericPipe();
    expect(pipe).toBeTruthy();
  });
});
